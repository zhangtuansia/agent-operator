{
  "name": "@agent-operator/server",
  "version": "0.7.1",
  "description": "Standalone headless Dazi server (Bun runtime)",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "cowork-server": "src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agent-operator/core": "workspace:*",
    "@agent-operator/server-core": "workspace:*",
    "@agent-operator/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.0.0"
  }
}
