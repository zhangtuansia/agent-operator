{
  "name": "@agent-operator/mermaid",
  "version": "0.1.5a",
  "license": "Apache-2.0",
  "description": "Mermaid flowchart to styled SVG renderer with zinc monochrome palette",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test src/__tests__/",
    "samples": "bun run index.ts",
    "dev": "bun run dev.ts",
    "bench": "bun run bench.ts",
    "build": "bun run index.ts && mkdir -p dist && mv index.html dist/ && cp -r public/* dist/",
    "deploy": "bun run build && wrangler pages deploy dist --project-name cowork-mermaid"
  },
  "dependencies": {
    "@dagrejs/dagre": "^1.1.8"
  },
  "devDependencies": {
    "shiki": "^3.19.0"
  }
}
