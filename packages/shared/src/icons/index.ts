/**
 * Icons module - re-exports all icon types and constants.
 *
 * Import via: `import { IconSize, ResolvedEntityIcon } from '@agent-operator/shared/icons'`
 */
export {
  type IconConfig,
  type ResolvedEntityIcon,
  type IconSize,
  ICON_SIZE_CLASSES,
  ICON_EMOJI_SIZES,
} from './types.ts'
