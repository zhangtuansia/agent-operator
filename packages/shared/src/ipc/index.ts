/**
 * IPC Module
 *
 * Provides types, schemas, and channel definitions for IPC communication
 * between main and renderer processes.
 */

// Re-export all schemas and inferred types
export * from './schemas'

// Re-export all channel definitions
export * from './channels'

// Re-export all IPC types
export * from './types'
